from fastapi import APIRouter

TAGS_METADATA = [
    {
        "name": "Chatbot",
        "description": "Chatbot API for interacting with the chatbot."
    }
]

def route_setup(app: FastAPI):
    pass  # Setup other routes or configurations here
